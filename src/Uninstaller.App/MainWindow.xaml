<Window x:Class="Uninstaller.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Uninstaller.App.ViewModels"
        xmlns:v="clr-namespace:Uninstaller.App.Views"
        mc:Ignorable="d"
        Title="Uninstaller Phase 5A" Height="600" Width="900" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
            <v:DashboardView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ApplicationsViewModel}">
            <v:ApplicationsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:HistoryViewModel}">
            <v:HistoryView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RecoveryViewModel}">
            <v:RecoveryView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <v:SettingsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ApplicationDetailsViewModel}">
            <v:ApplicationDetailsView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:CleanupPlanViewModel}">
            <v:CleanupPlanView />
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar Navigation -->
        <Border Grid.Column="0" Background="#F0F0F0" BorderBrush="#DDDDDD" BorderThickness="0,0,1,0">
            <StackPanel Margin="0,20,0,0">
                <TextBlock Text="Uninstaller" FontSize="20" FontWeight="Bold" Margin="20,0,20,20" />
                <Button Content="Dashboard" Command="{Binding NavigateToDashboardCommand}" Margin="10,5" Padding="10" HorizontalContentAlignment="Left" />
                <Button Content="Applications" Command="{Binding NavigateToApplicationsCommand}" Margin="10,5" Padding="10" HorizontalContentAlignment="Left" />
                <Button Content="History" Command="{Binding NavigateToHistoryCommand}" Margin="10,5" Padding="10" HorizontalContentAlignment="Left" />
                <Button Content="Recovery" Command="{Binding NavigateToRecoveryCommand}" Margin="10,5" Padding="10" HorizontalContentAlignment="Left" />
                <Button Content="Settings" Command="{Binding NavigateToSettingsCommand}" Margin="10,5" Padding="10" HorizontalContentAlignment="Left" />
            </StackPanel>
        </Border>

        <!-- Main Content Area -->
        <ContentControl Grid.Column="1" Content="{Binding CurrentViewModel}" Margin="10" />
    </Grid>
</Window>
