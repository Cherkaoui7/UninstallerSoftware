<Window x:Class="Uninstaller.App.Windows.UninstallConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:Uninstaller.App.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:UninstallConfirmationViewModel}"
        mc:Ignorable="d"
        Title="Confirm Uninstall" Height="300" Width="450" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Are you sure you want to run the official uninstaller for this application?" FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,15" />

        <Border Grid.Row="1" Background="#F0F0F0" Padding="10" CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Name:" FontWeight="SemiBold" Margin="0,0,10,5"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ApplicationName}" Margin="0,0,0,5" TextTrimming="CharacterEllipsis"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Version:" FontWeight="SemiBold" Margin="0,0,10,5"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Version}" Margin="0,0,0,5" TextTrimming="CharacterEllipsis"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Publisher:" FontWeight="SemiBold" Margin="0,0,10,5"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Publisher}" Margin="0,0,0,5" TextTrimming="CharacterEllipsis"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Method:" FontWeight="SemiBold" Margin="0,10,10,5"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding ExecutionMethod}" Margin="0,10,0,5"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Executable:" FontWeight="SemiBold" Margin="0,0,10,5"/>
                <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding ExecutablePath}" Margin="0,0,0,5" TextWrapping="WrapWithOverflow" FontFamily="Consolas" FontSize="11" />

                <TextBlock Grid.Row="5" Grid.Column="0" Text="Arguments:" FontWeight="SemiBold" Margin="0,0,10,5"/>
                <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding Arguments}" Margin="0,0,0,5" TextWrapping="WrapWithOverflow" FontFamily="Consolas" FontSize="11" />

                <TextBlock Grid.Row="6" Grid.Column="0" Text="Elevation:" FontWeight="SemiBold" Margin="0,10,10,0" Foreground="DarkRed" Visibility="{Binding RequiresElevation, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                <TextBlock Grid.Row="6" Grid.Column="1" Text="Requires Administrator privileges" Margin="0,10,0,0" Foreground="DarkRed" Visibility="{Binding RequiresElevation, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </Grid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Confirm" Width="80" Margin="0,0,10,0" Click="Confirm_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
