<UserControl x:Class="Uninstaller.App.Views.ApplicationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Scan" Command="{Binding ScanCommand}" Width="100" Margin="0,0,10,0" Padding="5" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" Margin="0,0,10,0" Padding="5" />
            <Button Content="View Details" Command="{Binding ViewDetailsCommand}" Width="100" Margin="0,0,10,0" Padding="5" />
            
            <TextBlock Text="Search:" VerticalAlignment="Center" Margin="10,0,5,0" />
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="200" VerticalAlignment="Center" Padding="2" />
        </StackPanel>

        <!-- Applications Grid -->
        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding ApplicationsView}" 
                  SelectedItem="{Binding SelectedApplication, Mode=TwoWay}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="Version" Binding="{Binding Version}" Width="*" />
                <DataGridTextColumn Header="Publisher" Binding="{Binding Publisher}" Width="1.5*" />
                <DataGridTextColumn Header="Status" Binding="{Binding UninstallStatus}" Width="*" />
                <DataGridTextColumn Header="Install Location" Binding="{Binding InstallLocation}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" FontWeight="Bold" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
