<UserControl x:Class="Uninstaller.App.Views.RecoverySessionHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:Uninstaller.App.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="&lt; Back" Command="{Binding GoBackCommand}" HorizontalAlignment="Left" Padding="10,5" Margin="0,0,0,10"/>
        
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="{Binding SessionDetails.ApplicationName, StringFormat='Recovery Session: {0}'}" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding SessionDetails.Timestamp, StringFormat='Started: {0}'}"/>
            <TextBlock Text="{Binding SessionDetails.Status, StringFormat='Status: {0}'}"/>
            <TextBlock Text="{Binding SessionDetails.TotalItems, StringFormat='Total Items: {0}'}"/>
            <TextBlock Text="{Binding SessionDetails.SuccessfulItems, StringFormat='Successful: {0}'}"/>
            <TextBlock Text="{Binding SessionDetails.FailedItems, StringFormat='Failed: {0}'}"/>
        </StackPanel>

        <ListView Grid.Row="2" ItemsSource="{Binding Items}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Type" DisplayMemberBinding="{Binding ArtifactType}"/>
                    <GridViewColumn Header="Path" DisplayMemberBinding="{Binding Path}"/>
                    <GridViewColumn Header="Classification" DisplayMemberBinding="{Binding Classification}"/>
                    <GridViewColumn Header="Outcome" DisplayMemberBinding="{Binding Outcome}"/>
                    <GridViewColumn Header="Failure Reason" DisplayMemberBinding="{Binding FailureReason}"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
