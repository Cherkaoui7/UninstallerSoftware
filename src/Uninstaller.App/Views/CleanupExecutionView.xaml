<UserControl x:Class="Uninstaller.App.Views.CleanupExecutionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uninstaller.App.Views"
             xmlns:vm="clr-namespace:Uninstaller.App.ViewModels"
             xmlns:enums="clr-namespace:Uninstaller.App.Enums"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:CleanupExecutionViewModel, IsDesignTimeCreatable=False}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        
        <Style x:Key="CancelBtnStyle" TargetType="Button" BasedOn="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
            <Setter Property="Background" Value="#FFF0F0"/>
            <Setter Property="Foreground" Value="#CC0000"/>
            <Setter Property="BorderBrush" Value="#CC0000"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FinishBtnStyle" TargetType="Button" BasedOn="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
            <Setter Property="Visibility" Value="Collapsed"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding State}" Value="{x:Static enums:UIState.Success}">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="{x:Static enums:UIState.Warning}">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="{x:Static enums:UIState.Error}">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="{x:Static enums:UIState.Cancelled}">
                    <Setter Property="Visibility" Value="Visible"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#F0F0F0" Padding="15" CornerRadius="5" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="Cleanup Execution" FontSize="24" FontWeight="SemiBold" Foreground="#333333"/>
                    <TextBlock Text="{Binding ApplicationName}" FontSize="16" Foreground="#666666" Margin="0,5,0,0"/>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="14" Foreground="#0066CC" Margin="0,5,0,0" FontWeight="Medium"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Top">
                    <TextBlock Text="{Binding StartedAt, StringFormat='Started: {0:HH:mm:ss}'}" FontSize="12" Foreground="#888888" HorizontalAlignment="Right"/>
                    <TextBlock Text="{Binding SessionId, StringFormat='Session: {0}'}" FontSize="10" Foreground="#AAAAAA" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Progress and Stats -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock FontSize="16" FontWeight="SemiBold">
                    <Run Text="{Binding CompletedCount, Mode=OneWay}" />
                    <Run Text=" / " />
                    <Run Text="{Binding TotalCount, Mode=OneWay}" />
                    <Run Text=" items processed" />
                </TextBlock>
            </StackPanel>
            
            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="{Binding SuccessCount, StringFormat='Success: {0}'}" Foreground="Green" Margin="0,0,15,0" FontWeight="Bold"/>
                <TextBlock Text="{Binding FailedCount, StringFormat='Failed: {0}'}" Foreground="Red" Margin="0,0,15,0" FontWeight="Bold"/>
                <TextBlock Text="{Binding SkippedCount, StringFormat='Skipped: {0}'}" Foreground="DarkOrange" Margin="0,0,15,0" FontWeight="Bold"/>
                <TextBlock Text="{Binding CancelledCount, StringFormat='Cancelled: {0}'}" Foreground="Gray" FontWeight="Bold"/>
            </StackPanel>

            <ProgressBar Grid.Row="1" Grid.ColumnSpan="2" Height="8" Minimum="0" Maximum="100" Value="{Binding ProgressPercentage, Mode=OneWay}" />
        </Grid>

        <!-- Items List -->
        <Border Grid.Row="2" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4">
            <ListView ItemsSource="{Binding Items}" 
                      BorderThickness="0" 
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      HorizontalContentAlignment="Stretch">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#EEEEEE" BorderThickness="0,0,0,1" Padding="10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="150" />
                                </Grid.ColumnDefinitions>

                                <!-- Type -->
                                <Border Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Background="#E3F2FD" CornerRadius="3" Padding="4" HorizontalAlignment="Left" VerticalAlignment="Top">
                                    <TextBlock Text="{Binding ArtifactType}" FontSize="11" Foreground="#1565C0" FontWeight="SemiBold"/>
                                </Border>

                                <!-- Path -->
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Path}" TextWrapping="Wrap" Margin="10,0,0,0" FontSize="13" Foreground="#444444" VerticalAlignment="Center"/>

                                <!-- Error Message (if any) -->
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding FailureReason}" TextWrapping="Wrap" Margin="10,5,0,0" FontSize="12" Foreground="Red" FontStyle="Italic">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding FailureReason, Converter={x:Null}}" Value="False">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <!-- State -->
                                <TextBlock Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" Text="{Binding DisplayState}" FontWeight="SemiBold" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#666666"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding DisplayState}" Value="Success">
                                                    <Setter Property="Foreground" Value="Green"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding DisplayState}" Value="Failed">
                                                    <Setter Property="Foreground" Value="Red"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding DisplayState}" Value="Skipped">
                                                    <Setter Property="Foreground" Value="DarkOrange"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding DisplayState}" Value="Waiting...">
                                                    <Setter Property="Foreground" Value="#AAAAAA"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <!-- Actions -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel Cleanup" 
                    Command="{Binding CancelCommand}"
                    Width="120" Height="30" Margin="0,0,10,0"
                    Style="{StaticResource CancelBtnStyle}" />
            
            <Button Content="Finish &amp; View History" 
                    Width="140" Height="30"
                    Background="#0078D7" Foreground="White" BorderThickness="0"
                    Style="{StaticResource FinishBtnStyle}" />
        </StackPanel>
    </Grid>
</UserControl>
